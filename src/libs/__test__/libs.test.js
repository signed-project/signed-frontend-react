import { generateId } from '../signature';

it('sums numbers', () => {
  expect(generateId()).toMatch(/\w/);
});

describe('signature',

it('[getJsonStringFromObj] get string ', () => {
    expect(generateId()).toMatch(/\w/);
  }),

  it('sums numbers', () => {
    expect(generateId()).toMatch(/\w/);
  }),

  it('sums numbers', () => {
    expect(generateId()).toMatch(/\w/);
  })
);

// getRegisterUserData()
// validate()
// post model()
// source model()
// getHash()


/*
describe('Circle class', function() {
  describe('area is calculated when', function() {
    it('sets the radius', function() { ... });
    it('sets the diameter', function() { ... });
    it('sets the circumference', function() { ... });
  });
});


const myBeverage = {
  delicious: true,
  sour: false,
};

describe('my beverage', () => {
  test('is delicious', () => {
    expect(myBeverage.delicious).toBeTruthy();
  });

  test('is not sour', () => {
    expect(myBeverage.sour).toBeFalsy();
  });
});
*/